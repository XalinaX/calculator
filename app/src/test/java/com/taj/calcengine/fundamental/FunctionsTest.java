package com.taj.calcengine.fundamental;

import org.junit.Test;

import static org.junit.Assert.*;

public class FunctionsTest {

    @Test
    public void pow() {
//        Rational r = new Rational(4, 9);
//        Rational power = new Rational(3);
//        assertEquals(new Rational(64, 729),Functions.pow(r, power));
//
//        power = new Rational(1, 2);
//        assertEquals(new Rational(2, 3), Functions.pow(r, power));
//
//        power = new Rational(1, 3);
//        assertEquals(Math.pow(4.0 / 9, 1.0 / 3), Functions.pow(r, power).toDouble(), 0.0001);
//
//        power = new Rational(2, 3);
//        assertEquals(Math.pow(4.0 / 9, 2.0 / 3), Functions.pow(r, power).toDouble(), 0.0001);
    }
}